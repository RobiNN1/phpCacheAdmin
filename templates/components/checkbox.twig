<input type="checkbox"
       class="checkbox text-{{ color }}-500 -mt-1.5 h-4 w-4 select-none appearance-none rounded border border-gray-300 bg-white align-middle
       focus:outline-none focus:ring focus:ring-{{ color }}-200 focus:border-{{ color }}-300{{ class|space }}"
       aria-label=""{{ disabled ? ' disabled' : '' }}>
