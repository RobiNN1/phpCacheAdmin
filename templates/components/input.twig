<div class="{{ inline_input ? 'flex gap-2 w-full items-center' : 'mb-4' }}"{{ extra_attr|raw }}>
    <label for="{{ id }}" class="{{ inline_input ? '' : 'mb-2 ' }}block text-sm font-semibold">{{ label|raw }}</label>
    <input id="{{ id }}" name="{{ id }}" type="{{ type ? type : 'text' }}" value="{{ value }}"
           class="text-sm w-full rounded border border-gray-300 bg-white py-1.5 px-3 focus:outline-hidden focus:ring-3 focus:ring-primary-200 focus:border-primary-300
           dark:border-gray-600 dark:bg-gray-800 dark:text-white dark:focus:ring-primary-500 dark:focus:border-primary-400"{{ required ? ' required' : '' }}{{ attr|raw }}>
    {% if help %}
        <small class="text-xs">{{ help }}</small>
    {% endif %}
</div>
