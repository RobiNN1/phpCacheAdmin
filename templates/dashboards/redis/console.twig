{{ include('partials/console.twig', {
    prompt: 'redis:' ~ get('db', 0) ~ '>',
    commands_url: asset('assets/redis-commands.json'),
    welcome: 'Type a Redis command and press Enter (e.g. <span class="font-semibold">PING</span>,
        <span class="font-semibold">SET foo bar</span>, <span class="font-semibold">KEYS *</span>).
        Type <span class="font-semibold">clear</span> to reset the output.',
    note: 'Each command runs on a fresh connection using the currently selected database.
        Blocking and connection-level commands (MONITOR, SUBSCRIBE, BLPOP, ...) are disabled.
        Use &uarr;/&darr; for command history and &#8677; Tab to complete command names.',
}) }}
