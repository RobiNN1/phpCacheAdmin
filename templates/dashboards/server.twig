<div class="mb-5 text-lg font-semibold">Loaded PHP extensions ({{ extensions|length }})</div>

<div>
    {% for extension in extensions %}
        {{ include('components/badge.twig', {
            text: extension,
            pill: true,
            txt: 'font-semibold',
            bg: 'bg-slate-200',
            class: 'm-1',
            link: ext_link ~ '#module_' ~ extension|replace({' ': '+'})|lower,
        }) }}
    {% endfor %}
</div>

<small class="text-xs">You can click on these badges, and it will redirect you to the appropriate section in phpinfo().</small>
