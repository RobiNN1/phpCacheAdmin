{% for panel in panels %}
    {% if not panel.error %}
        {{ include('partials/panel.twig', {
            panel_title: panel.title,
            array: panel.data,
            moreinfo: panel.moreinfo,
        }) }}
    {% else %}
        {{ panel.error }}
    {% endif %}
{% endfor %}
