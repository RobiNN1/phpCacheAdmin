{% for panel in panels %}
    {% if panel %}
        {% if not panel.error %}
            {{ include('partials/panel.twig', {
                panel_title: panel.title,
                array: panel.data,
            }) }}
        {% else %}
            {{ panel.error }}
        {% endif %}
    {% endif %}
{% endfor %}
