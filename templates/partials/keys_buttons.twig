{{ include('components/button.twig', {
    text: 'Import',
    icon: 'import',
    id: 'import_btn',
    btn_green: true,
}) }}

{{ include('components/button.twig', {
    text: 'Add new key',
    icon: 'open',
    link: new_key_url,
    btn_green: true,
}) }}

{% if all_keys != 0 %}
    {{ include('components/button.twig', {
        text: 'Delete all keys',
        icon: 'trash',
        id: 'delete_all',
        btn_red: true,
    }) }}

    {{ include('components/button.twig', {
        text: 'Delete selected',
        icon: 'trash',
        id: 'delete_selected',
        btn_red: true,
    }) }}
{% endif %}
